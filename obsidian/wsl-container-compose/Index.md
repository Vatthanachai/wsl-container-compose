# WSL Container Compose

A docker-compose/podman-compose equivalent for Windows, built in C# .NET 10, orchestrating real containers via Microsoft's native WSL container feature (`Microsoft.WSL.Containers`).

## Notes

- [[Plan]] — design decisions reached, and why
- [[Project Structure]] — solution/project layout
- [[Progress]] — current status
- [[How To]] — getting started, prerequisites, workflows
- [[References]] — external docs and links
